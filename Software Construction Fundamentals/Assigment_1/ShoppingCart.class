import java.util.ArrayList;
import java.
class ShoppingCart extends Item{
            
            ArrayList<Item> itemsList= new ArrayList<Item>();
            
            void addItem(Item item ,int quantity)
            {
                    item = new Item(item,quantity);
            } 

            int displayQty(Item item)
            {
                    return item.Quantity;
            }
            void deleteItem(Item item)
            {
                if(item.Quantity ==0)
                  throw new Exception("No Item Occurance to be removed !!");
                item.Quantity--;
            }

            void updateQty(Item item,Integer quantity)
            {
                item.Quantity =item.Quantity + quantity;
            }

            Double displayBill()
            {
                // will i have to iterate each item 
            }




    }